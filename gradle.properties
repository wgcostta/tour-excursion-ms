# ==============================================
# GRADLE BUILD CONFIGURATION
# ==============================================

# Gradle Build Performance
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true
org.gradle.workers.max=4

# Gradle Build Cache
org.gradle.configuration-cache=true
org.gradle.configuration-cache.problems=warn

# Kotlin DSL (não aplicável, mas deixado para referência)
# kotlin.code.style=official

# Spring Boot Build Info
springBoot.buildInfo.properties.additional.encoding=UTF-8

# Application Properties
app.name=tour-application
app.version=1.0.0
app.description=Tour Management API for organizing and managing travel tours

# Build Optimization
org.gradle.vfs.watch=true
org.gradle.unsafe.configuration-cache=false